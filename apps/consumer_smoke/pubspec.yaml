name: steady_async_consumer_smoke
description: Local-path platform smoke consumer for steady_async.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  steady_async:
    path: ../../packages/steady_async

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
